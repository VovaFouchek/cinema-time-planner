import Card from '@components/Card';

// import styles from './meetings.module.scss';

const Meetings = () => {
  return (
    <Card>
      <h2>Meetings</h2>
    </Card>
  );
};

export default Meetings;
